import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        marginTop: 20,
        width: 179,
        height: 44,
        borderRadius: 20,
        borderColor: '#fbc402',
        borderWidth: 2,
        overflow: 'hidden',
    },
    content: {
        textAlign: 'center',
        color: '#c2030b',
        fontWeight: 'bold'
    },
    gradient: {
        flex :1,
        justifyContent: "center",
        alignItems: "center",
        borderRadius: 25,
      },
});
